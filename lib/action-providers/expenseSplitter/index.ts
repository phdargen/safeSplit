/**
 * Expense Splitter Action Provider
 * 
 * Provides AgentKit actions for tracking expenses and settling them via USDC transfers.
 */

export { expenseSplitterActionProvider } from "./expenseSplitterActionProvider";
export type { MultiTransactionPrepared } from "./types";

